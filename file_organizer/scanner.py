def scan_directory(directory_path):
    """
    Scans the given directory and returns metadata about files.

    Args:
        directory_path (str): Path to the directory to scan

    Returns:
        list: A list of dictionaries containing file metadata
    """
    pass
