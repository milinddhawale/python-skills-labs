import os

def scan_directory(directory_path):
    """
    Scan the given directory and return metadata
    about files and directories.

    Args:
        directory_path (str): Path to scan

    Returns:
        list: List of dictionaries with item metadata
    """
    pass
